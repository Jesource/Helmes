package ee.helmes.home.assignment.enums;

public enum CustomHttpSessionAttributes {
    SUBMITTED_USER_DATA_ID; // Used to check if user has previously filled and submitted their details
                            // and there is a saved record containing that data
}
